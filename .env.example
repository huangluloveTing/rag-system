# ================================
# 项目级环境变量说明
# ================================
#
# 本项目采用独立的环境变量配置，每个应用维护自己的 .env 文件：
#
# 📁 apps/backend/.env
#    - 后端服务配置（数据库、Redis、Qdrant、MinIO、LLM 等）
#
# 📁 apps/frontend/.env
#    - 前端应用配置（API 地址、端口等）
#
# 📁 apps/embedding/.env
#    - Embedding 服务配置（本地模型或外部 API）
#
# ================================
# 快速开始
# ================================
#
# 1. 复制各项目的 .env.example 到 .env（如果还没有）
# 2. 根据实际环境修改各项目的 .env 文件
# 3. 确保 Embedding API Key、LLM API Key 等敏感信息已配置
#
# ================================
# 重要提示
# ================================
#
# - 不要将 .env 文件提交到 Git
# - 生产环境请使用环境变量或密钥管理服务
# - 每个服务的配置独立，互不影响

# ================================
# 全局默认配置（可选）
# ================================
NODE_ENV=development